{
  "name": "livis.no",
  "private": true,
  "version": "1.0.0",
  "scripts": {
		"start": "static-server src/ --open",
    "deploy": "gh-pages --dist src/ --branch gh-pages"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "gh-pages": "^2.1.1",
    "static-server": "^2.2.1"
  }
}
